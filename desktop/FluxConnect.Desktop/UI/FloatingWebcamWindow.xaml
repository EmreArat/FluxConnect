<Window x:Class="FluxConnect.Desktop.UI.FloatingWebcamWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        Width="240" Height="180"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        FontFamily="{StaticResource MainFont}">

    <Border x:Name="RootBorder"
            CornerRadius="12"
            Background="{StaticResource FloatingPanelBrush}"
            BorderBrush="{StaticResource FloatingBorderBrush}"
            BorderThickness="1"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="28"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Başlık Çubuğu (Sürükleme + Kapat) -->
            <Grid Grid.Row="0" Margin="6,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📷"
                               FontSize="11"
                               VerticalAlignment="Center"
                               Margin="0,0,5,0"/>
                    <TextBlock x:Name="TxtTitle"
                               Text="Karşı Tarafın Kamerası"
                               Foreground="{StaticResource TextSubtleOnDarkBrush}"
                               FontSize="10"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Kapat Butonu -->
                <Button x:Name="BtnClose"
                        Content="✕"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Width="18" Height="18"
                        FontSize="9"
                        Padding="0"
                        Foreground="{StaticResource TextMutedOnDarkBrush}"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Cursor="Hand"
                        Click="BtnClose_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Bd"
                                                Background="{TemplateBinding Background}"
                                                CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource DangerSoftBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Webcam Görüntüsü -->
            <Border Grid.Row="1"
                    CornerRadius="0,0,12,12"
                    Background="Black"
                    ClipToBounds="True">
                <Image x:Name="ImgWebcam"
                       Stretch="UniformToFill"/>
            </Border>

            <!-- Bağlantı Bekleniyor Overlay -->
            <Border Grid.Row="1"
                    x:Name="WaitingOverlay"
                    Background="{StaticResource OverlayScrimBrush}"
                    CornerRadius="0,0,12,12">
                <TextBlock Text="Kamera bekleniyor..."
                           Foreground="{StaticResource TextMutedOnDarkBrush}"
                           FontSize="11"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>
        </Grid>
    </Border>
</Window>
